name: CI Skip

on:
  pull_request:
    paths:
      - '**/*.md'
      - 'docs/**'
      - '.gitignore'
      - 'README.md'

jobs:
  test:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Skipping tests"
